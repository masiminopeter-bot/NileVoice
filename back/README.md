# NileVoice Backend

This folder contains the application and deployment files for the NileVoice backend service.

## Deployment
- Render will install dependencies from requirements.txt.
- Render will start the app with Procfile.
- Configure database and JWT environment variables in Render.
